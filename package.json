{
  "name": "neualto-website",
  "version": "1.0.0",
  "private": true,
  "description": "NeuAlto Technologies marketing site. Static, no build step - this file is a script registry for the checkers, not a toolchain. It has no dependencies and nothing here transforms the served files.",
  "scripts": {
    "test": "node scripts/check-seo.js && node scripts/check-chrome.js && node scripts/check-posts.js && node scripts/assistant-widget.test.js",
    "seo": "node scripts/check-seo.js",
    "chrome": "node scripts/check-chrome.js",
    "posts": "node scripts/check-posts.js",
    "kb": "node scripts/build-kb-docs.js",
    "start": "npx http-server -p 4173 -c-1 ."
  },
  "license": "UNLICENSED"
}
